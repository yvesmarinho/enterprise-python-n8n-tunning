# N8N Performance Report ANA-001 — Análise Aprofundada

**Gerado em**: 2026-03-31
**Período analisado**: 2026-01-01 → 2026-03-31 (90 dias)
**Fonte de dados**: VictoriaMetrics (wfdb01) via SSH tunnel — histórico completo
**Analista**: enterprise-python-analysis / ANA-001
**Status**: ANÁLISE APROFUNDADA — dados brutos consultados diretamente

---

## 1. Resumo Executivo

| Métrica | Valor |
|---------|-------|
| Execuções totais — série `job=n8n, instance=wf001` (scrape direto) | **148.058** |
| Execuções totais — série `job=collector_api_wf001_usa, instance=0.0.0.0:5000` (Pushgateway) | **366.303** |
| ⚠️ Mesma fonte (`prod-collector-api`), dois caminhos de coleta ativos simultaneamente | ver nota abaixo |
| Workflows distintos ativos | **23** |
| p95 de duração individual | **< 0,1s** (todos os workflows) |
| Violações p95 ≥ 1s | **0** |
| Workflow de maior volume | **121Labs PABX call-analytics** (429.786 exec) |
| Workflow de crescimento súbito | **hub-whatsapp-api-gateway-evolution-api** (84.639 exec desde 04/mar) |
| CPU wf001 durante picos | **1–3%** (não é gargalo de hardware) |

**Conclusão principal:** Os workflows N8N individualmente são rápidos — todos completam em menos de 100ms. O "problema de desempenho" percebido **não está na duração de execução individual**, mas sim no **volume de execuções** de um único cliente e no **tempo de fila não instrumentado**.

### Ações Imediatas Recomendadas

| # | Prioridade | Ação | Responsável | Prazo |
|---|-----------|------|-------------|-------|
| 1 | � P2 | **Auditar `hub-whatsapp-api-gateway-evolution-api`** — 84K exec desde 04/mar (Evolution API gateway WhatsApp), verificar rate-limiting e se o volume é esperado pela integração | N8N Admin | Esta semana |
| 2 | 🔴 P1 | **Habilitar métricas de fila N8N** (`N8N_METRICS_INCLUDE_QUEUE_METRICS=true`) para medir tempo de espera real | DevOps/wf001 | Esta semana |
| 3 | ✅ Resolvido | **Relabeling do exporter N8N** — `instance=0.0.0.0:5000` é herança histórica do `prod-collector-api` antes da remapagem de porta. Config atual (`5001→5000` + `replacement: wf001`) já está correta | DevOps/Prometheus | *Concluído* |
| 4 | 🟡 P2 | **Analisar PostgreSQL do N8N** — 429K execuções podem estar sobrecarregando `execution_entity`; verificar purgação | DBA | 2 semanas |
| 5 | 🟡 P2 | **Configurar purgação de execuções** (`EXECUTIONS_DATA_PRUNE=true`, retenção ≤ 30 dias) | DevOps/wf001 | 2 semanas |
| 6 | 🟢 P3 | **Avaliar batching do 121Labs PABX** — agrupar eventos por janela de 1 min reduz volume em 60× | 121Labs / N8N |  30 dias |
| 7 | 🟢 P3 | **Adicionar buckets sub-100ms ao histograma** (`0.005, 0.01, 0.025, 0.05`) para medir latência real | DevOps/N8N | 30 dias |
| 8 | 🟢 P3 | **Probe sintético end-to-end** — medir tempo desde webhook até resposta para capturar fila + execução | DevOps | 30 dias |

---

## 2. Diagnóstico de Latência Individual

### 2.1 Distribuição de duração (histograma)

O histograma `n8n_workflow_execution_duration_seconds_bucket` possui os seguintes buckets configurados:

`0.1s | 0.5s | 1.0s | 5.0s | 10.0s | 30.0s | 60.0s | 120.0s | 300.0s | 600.0s | +Inf`

**Resultado observado em 90 dias para os workflows de maior volume:**

| Workflow | Execuções (30d) | ≤ 0,1s | > 0,1s | p95 |
|----------|----------------|--------|--------|-----|
| 121Labs PABX call-analytics | 294.383 | **100%** | 0% | 0,095s |
| hub-whatsapp-api-gateway-evolution-api | 40.619 | **100%** | 0% | 0,095s |
| hub-whatsapp-api-validate-client | 2.959 | **100%** | 0% | 0,095s |
| ai-agentbot-bridge-safra-vcom-zap2go | 3.994 | **100%** | 0% | 0,095s |
| enterprise-execute-queue | 369 | **100%** | 0% | 0,095s |

**Interpretação:** 100% das execuções completam em menos de 100ms. Os workflows são individualmente eficientes. O histograma confirma que não há execuções lentas individuais — o bucket `0.1s` absorve tudo.

> ⚠️ **Limitação de instrumentação:** O p95 exibido como `0.095s` é o limite superior do bucket `le=0.1` — não é a latência real medida por interpolação. Não há dados de resolução abaixo de 100ms. Para diagnóstico de latência sub-100ms, seria necessário adicionar buckets menores (ex: `0.005, 0.01, 0.025, 0.05`).

---

## 3. Ofensores de Volume (Throughput)

### 3.1 Ranking de execuções — 90 dias

> **Nota sobre dupla coleta — CORRIGIDA:** O `prod-collector-api` envia métricas por **dois caminhos simultâneos e ativos**:
> - **Caminho A — scrape direto:** Prometheus raspa `31.220.103.208:5001` → relabeling → `job=n8n, instance=wf001`
> - **Caminho B — Pushgateway:** `prod-collector-api` empurra as mesmas métricas para o Pushgateway a cada 60s → Prometheus raspa o Pushgateway → `job=collector_api_wf001_usa, instance=0.0.0.0:5000`
>
> Ambos os caminhos reportam os **mesmos contadores de execuções do N8N**. A série `0.0.0.0:5000` **não é herança histórica** — é um push ativo agora. As séries cobrem períodos diferentes (Caminho B tem histórico maior), por isso `increase([90d])` retorna valores distintos. **O ProvenanceGate está correto ao filtrar uma das séries** — sem ele, `sum()` entre as duas duplicaria a contagem. A tabela abaixo usa os valores brutos de cada série separadamente.

| # | Workflow | Instance (série) | Execuções (90d) |
|---|----------|------------------|-----------------|
| 1 | **121Labs PABX call-analytics** | 0.0.0.0:5000 (Pushgateway) | 294.162 |
| 2 | **121Labs PABX call-analytics** | wf001 (scrape direto) | 135.624 |
| 3 | **hub-whatsapp-api-gateway-evolution-api** | 0.0.0.0:5000 (Pushgateway) | 72.205 |
| 4 | **hub-whatsapp-api-gateway-evolution-api** | wf001 (scrape direto) | 12.434 |
| 5 | enterprise-trigger-cron-suporte-crm | 0.0.0.0:5000 (Pushgateway) | 3.793 |
| 6 | ai-agentbot-bridge-safra-vcom-zap2go | ambas | 3.994 |
| 7 | hub-whatsapp-api-validate-client | ambas | 2.959 |
| — | demais workflows | — | ~5.509 |

**Para comparar volumes entre workflows, use apenas UMA série** (ex: `0.0.0.0:5000` para maior janela histórica). O ranking consolidado abaixo usa o maior valor disponível por workflow:

| # | Workflow | Execuções best-effort (90d) | Série usada |
|---|----------|-----------------------------|-------------|
| 1 | **121Labs PABX call-analytics** | **294.162** | Pushgateway (90d completo) |
| 2 | **hub-whatsapp-api-gateway-evolution-api** | **84.639** | Pushgateway (90d completo) |
| 3 | enterprise-trigger-cron-suporte-crm | 3.793 | Pushgateway |
| 4 | ai-agentbot-bridge-safra-vcom-zap2go | 3.994 | consolidado |
| 5 | hub-whatsapp-api-validate-client | 2.959 | consolidado |

<details>
<summary>▶ Dados brutos por instance label (pré-consolidação)</summary>

| # | Workflow | Instance | Execuções |
|---|----------|----------|----------|
| 1 | 121Labs PABX call-analytics | 0.0.0.0:5000 (histórico) | 294.162 |
| 2 | 121Labs PABX call-analytics | wf001 (atual) | 135.624 |
| 3 | hub-whatsapp-api-gateway-evolution-api | 0.0.0.0:5000 (histórico) | 72.205 |
| 4 | hub-whatsapp-api-gateway-evolution-api | wf001 (atual) | 12.434 |
| 5 | enterprise-trigger-cron-suporte-crm | 0.0.0.0:5000 (histórico) | 3.793 |

</details>

> ✅ **Dupla coleta confirmada (2026-03-31):** `instance=0.0.0.0:5000` é o caminho via Pushgateway do `prod-collector-api` — **ativo agora**, não herança histórica. O mesmo container envia métricas por dois caminhos simultâneos (`job=n8n` via scrape direto + `job=collector_api_wf001_usa` via Pushgateway). ProvenanceGate correto ao recusar `0.0.0.0:5000` — evita dupla contagem. **Ação necessária:** desabilitar um dos dois caminhos (recomendado: manter scrape direto, desabilitar `PROMETHEUS_PUSHGATEWAY_ENABLED=true` no `prod-collector-api`).

---

### 3.2 Perfil de Carga — 121Labs PABX call-analytics

**Volume diário (dias com atividade significativa):**

| Data | Execuções | Pico (exec/hora) | Janela ativa |
|------|-----------|------------------|--------------|
| 2026-03-05 | 3.023 | ~500 | pontual |
| 2026-03-12 | 16.143 | ~2.500 | tarde |
| 2026-03-13 | 3.772 | ~700 | tarde |
| 2026-03-21 | 50.448 | ~7.992 | 15:00–23:00 UTC |
| 2026-03-24 | 44.262 | ~8.276 | 13:00–22:00 UTC |
| 2026-03-25 | 69.072 | **~8.372** | 13:00–16:00 UTC |
| 2026-03-26 | 25.257 | ~8.268 | 14:00–22:00 UTC |
| 2026-03-27 | 65.775 | **~8.416 (PICO MÁXIMO)** | 13:00–22:00 UTC |
| 2026-03-28 | 57.384 | ~8.000 | 13:00–21:00 UTC |
| 2026-03-30 | ~43.000 | ~7.832 | 14:00–22:00 UTC |
| 2026-03-31 | ~49.983 | ~7.776 | 13:00–19:00 UTC |

**Padrão identificado:** Atividade concentrada de **13:00 às 22:00 UTC (10:00–19:00 BRT)** — horário comercial PABX. Pico máximo observado: **8.416 execuções/hora** (2026-03-27 20:00 UTC).

**Escala do problema:** 8.400 exec/hora = **140 exec/minuto = 2,3 exec/segundo** de forma contínua por até 9 horas.

---

### 3.3 Perfil de Carga — hub-whatsapp-api-gateway-evolution-api

Workflow identificado como **gateway WhatsApp via Evolution API** — integração de mensageria. O crescimento súbito pode refletir onboarding de novos clientes ou aumento de volume de mensagens processadas.

**Histórico de crescimento:**

| Semana | Execuções (todos os hosts) | Crescimento |
|--------|---------------------------|-------------|
| Até 2026-02-12 | 10 | baseline |
| 2026-02-13 a 2026-03-03 | 0 | inativo |
| **2026-03-04** | 151 | **APARECIMENTO SÚBITO** |
| 2026-03-05 | 2.958 | +1.860% |
| 2026-03-06 a 2026-03-08 | ~15.610 | escalada rápida |
| Semana 2026-03-12 | **33.782** | pico semanal |
| Semana 2026-03-19 | 13.315 | redução parcial |
| Semana 2026-03-26 | **21.516** | reaceleração |

**⚠️ ATENÇÃO:** Workflow de gateway WhatsApp (Evolution API) com 84K execuções em 27 dias. Volume alto para uma integração de mensageria — verificar se há rate-limiting configurado e se o volume é proporcional à base de clientes ativos.

---

## 4. Correlação com Infraestrutura wf001

### 4.1 CPU do Servidor

| Período | CPU Médio Diário | Contexto |
|---------|-----------------|----------|
| 2026-03-13 a 2026-03-20 | **0,6–1,3%** | pré-pico |
| 2026-03-21 a 2026-03-28 (pico) | **1,2–1,8%** | pico de 121Labs |
| 2026-03-31 | **1,8%** | atual |

**Conclusão:** A carga de CPU no wf001 **não aumentou proporcionalmente** ao volume de execuções. Durante o pico de 8.400 exec/hora, o CPU estava em apenas 1,5–1,8%. O servidor tem capacidade computacional sobressalente.

### 4.2 Diagnóstico da Causa-Raiz Real

O servidor **não está sobrecarregado em hardware**. Os workflows são rápidos individualmente. Isso aponta para uma das seguintes causas para a lentidão percebida:

| Hipótese | Evidência | Status |
|----------|-----------|--------|
| Fila de execução N8N saturada | 2,3 exec/s entrada; concorrência padrão N8N = 5–20; fila não instrumentada | ⚠️ **HIPÓTESE PRINCIPAL — NÃO CONFIRMÁVEL com métricas atuais** |
| Banco de dados PostgreSQL sobrecarregado | n8n armazena todo histórico de execução no PG; 429K execuções em 90 dias = carga real | ⚠️ **HIPÓTESE SECUNDÁRIA — requer análise DBA** |
| Webhook/trigger response time | Tempo entre disparo externo e resposta N8N inclui fila — não medido | ⚠️ **NÃO INSTRUMENTADO** |
| Gargalo de hardware (CPU/memória) | CPU 1-3%, memória sem dados (node_exporter sem label correto) | ✅ **DESCARTADO para CPU** |
| Workflows lentos individualmente | p95 < 0,1s em 100% das execuções | ✅ **DESCARTADO** |

---

## 5. Lacunas de Instrumentação Identificadas

| Lacuna | Impacto | Ação Necessária |
|--------|---------|----------------|
| `n8n_queue_depth` — profundidade da fila de execução N8N | Não é possível confirmar hipótese de fila saturada | Habilitar métricas de queue no N8N (`QUEUE_HEALTH_CHECK_ACTIVE=true`) |
| `n8n_concurrency_*` — execuções simultâneas ativas | Não sabemos se workers estão saturados | Expor via endpoint `/metrics` (N8N >= 0.214) |
| Tempo de início (wait time antes de executar) | Latência percebida pelo usuário não medida | Adicionar log estruturado de `waitDuration` por execução |
| `node_memory_*` do wf001 | Sem dados de memória do host | Corrigir label `instance` no scrape config Prometheus |
| `instance` relabeling no exporter N8N | ✅ Resolvido — scrape direto usa `replacement: wf001` | — |
| **Dupla coleta ativa** | `prod-collector-api` envia via scrape direto E via Pushgateway simultaneamente | Desabilitar `PROMETHEUS_PUSHGATEWAY_ENABLED=true` no `prod-collector-api` |
| `n8n_workflow_execution_duration_seconds_bucket` — sub-100ms | Não mensurável abaixo de 100ms | Adicionar buckets: `0.005, 0.01, 0.025, 0.05` |
| PostgreSQL query latency | Não monitorado | Ativar `pg_stat_statements` + `postgres_exporter` com queries stats |

---

## 6. Plano de Ação Priorizado

### P1 — Imediato (esta semana)

**Ação 1.1 — Auditar `hub-whatsapp-api-gateway-evolution-api`** (ID: `UBL7fEwM7ldJ8oGa`)
- Workflow identificado: gateway de mensagens WhatsApp via Evolution API
- Verificar: volume esperado vs. real, configuração de rate-limiting na Evolution API
- Verificar se o crescimento de 04/mar coincide com onboarding de novos clientes
- Critério de aceitação: volume documentado e justificado pela base de clientes ativa

**Ação 1.2 — Habilitar métricas de fila N8N**

Adicionar nas variáveis de ambiente do container N8N em `wf001`:
```env
QUEUE_BULL_REDIS_HOST=redis
QUEUE_HEALTH_CHECK_ACTIVE=true
N8N_METRICS=true
N8N_METRICS_INCLUDE_QUEUE_METRICS=true
N8N_DIAGNOSTICS_ENABLED=true
```

Em seguida verificar se `n8n_queue_*` aparece em `/metrics`.

**Ação 1.3 — Confirmar e documentar relabeling do `prod-collector-api`** ✅ *parcialmente resolvido*

A duplicação já está tratada no nível do Prometheus — o scrape config atual usa `31.220.103.208:5001` com `replacement: "wf001"`. A série `0.0.0.0:5000` é **herança histórica** da época em que o container era mapeado em `:5000→:5000` sem relabeling.

**Nenhuma ação de configuração é necessária** — o Prometheus já está correto. O que resta é:
1. Garantir que `prod-collector-api` **não seja remapeado de volta para porta 5000** no host (colide com `code_store`)
2. VictoriaMetrics continuará armazenando dados históricos com `instance=0.0.0.0:5000` — isso é esperado e não é um problema operacional
3. ProvenanceGate deve continuar rejeitando `0.0.0.0:5000` em análises novas para evitar dupla contagem

```yaml
# Estado ATUAL (correto):
job_name: "n8n"
static_configs:
  - targets: ["31.220.103.208:5001"]  # prod-collector-api
relabel_configs:
  - source_labels: [__address__]
    target_label: instance
    replacement: "wf001"  # ← correto
```

---

### P2 — Curto prazo (próximas 2 semanas)

**Ação 2.1 — Analisar PostgreSQL do N8N**

```sql
-- Verificar tabelas mais pesadas
SELECT schemaname, tablename, pg_size_pretty(pg_total_relation_size(schemaname||'.'||tablename))
FROM pg_tables WHERE schemaname = 'public'
ORDER BY pg_total_relation_size(schemaname||'.'||tablename) DESC LIMIT 10;

-- Verificar queries lentas
SELECT query, calls, mean_exec_time, total_exec_time
FROM pg_stat_statements ORDER BY mean_exec_time DESC LIMIT 20;
```

Execuções do N8N são armazenadas em `execution_entity` — 429K execuções em 90 dias podem deixar a tabela grande sem purgação.

**Ação 2.2 — Configurar purgação de execuções antigas**

No N8N, via `Settings > n8n Settings`:
```
Execution retention: 30 days (ou conforme política)
```
Ou via env:
```env
EXECUTIONS_DATA_MAX_AGE=168  # horas = 7 dias
EXECUTIONS_DATA_PRUNE=true
```

**Ação 2.3 — Avaliar throttling do 121Labs PABX**

O workflow `121Labs PABX call-analytics` processa eventos de PABX individualmente. Avaliar com o cliente:
- É possível agrupar eventos por janela de 1 minuto (batch) antes de disparar o workflow?
- Em vez de 8.400 exec/hora, uma janela de 1 min reduziria para 140 workflows de análise/hora
- Impacto: mesmo resultado analítico, 60x menos carga no N8N

---

### P3 — Médio prazo (próximos 30 dias)

**Ação 3.1 — Monitoramento sintético (probe) end-to-end**

Criar probe que dispara um webhook N8N a cada 5 minutos e mede o tempo de resposta completo:
```yaml
# blackbox exporter ou script cURL:
# tempo desde HTTP POST até receber 200 OK do webhook
# esta métrica captura fila + execução + overhead — a latência REAL percebida pelo cliente
```

**Ação 3.2 — Adicionar buckets sub-100ms ao histograma**

No exporter N8N (configuração do `prom-client`), adicionar:
```js
buckets: [0.005, 0.01, 0.025, 0.05, 0.1, 0.5, 1, 5, 10, 30, 60, 120, 300, 600]
```
Isso permite ver a distribuição real de latência abaixo de 100ms.

**Ação 3.3 — Separar workflows de alto volume em instância dedicada**

Para `121Labs PABX call-analytics` (429K exec/90d, 57% do total):
- Criar instância N8N dedicada em `wf002` ou container separado
- Isola o impacto desse cliente dos demais workflows internos
- Permite escalar independentemente (mais workers, mais memória)

---

## 7. Resposta à Pergunta Original

> "Qual é o ofensor do desempenho do N8N?"

**Resposta baseada nos dados:**

1. **O ofensor de volume é `121Labs PABX call-analytics`** — 429K execuções em 90 dias, picos de 8.400/hora. Não é lento individualmente, mas domina a fila do N8N durante o horário comercial.

2. **O segundo ofensor é `hub-whatsapp-api-gateway-evolution-api`** — gateway WhatsApp via Evolution API com 84K execuções desde 04/mar. Verificar se o volume é esperado e se há rate-limiting adequado.

3. **A lentidão percebida provavelmente é tempo de fila** — os workflows completam em < 100ms cada, mas se a fila tem 500+ entradas aguardando durante o pico, o tempo de espera pode ser de minutos. Esse dado não está instrumentado atualmente.

4. **O hardware não é o gargalo** — CPU em 1–3% confirma isso. O problema é de capacidade lógica do N8N (workers, fila, banco de dados).

---

## 8. Limitações desta Análise

| Limitação | Detalhe |
|-----------|---------|
| Tempo de fila não medido | A métrica mais importante para "lentidão percebida" não existe atualmente |
| Memória wf001 sem dados | `node_memory_*` retornou vazio — label `instance` incorreto no scrape |
| Histórico truncado | Dados de jan/fev são esparsos — execuções começaram a aparecer com volume em março |
| `instance=0.0.0.0:5000` excluído | ✅ **CONFIRMADO**: é o `prod-collector-api` em wf001 antes da remapagem de porta. ProvenanceGate correto — evita dupla contagem com série `wf001` atual |
| Nível de node não disponível | `n8n_node_execution_duration_seconds` ausente — não é possível ver qual nó interno de cada workflow é lento |

---

## Apêndice — Queries Utilizadas

| # | Fonte | Query |
|---|-------|-------|
| 1 | VictoriaMetrics | `sort_desc(sum by (workflow_name, instance) (increase(n8n_workflow_execution_duration_seconds_count[90d])))` |
| 2 | VictoriaMetrics | `sum(increase(...count{workflow_name="121Labs PABX call-analytics"}[1d]))` step=1d |
| 3 | VictoriaMetrics | `sum(rate(...count{workflow_name="121Labs PABX call-analytics"}[1h]))*3600` step=1h |
| 4 | VictoriaMetrics | `histogram_quantile(0.95, sum by (workflow_name, le) (rate(...bucket[30d])))` |
| 5 | VictoriaMetrics | `sum(increase(...bucket{le="0.1"}[30d]))` — distribuição por bucket |
| 6 | VictoriaMetrics | `avg(rate(node_cpu_seconds_total{mode!="idle"}[1d]))*100` step=1d |
| 7 | VictoriaMetrics | `group by (le) (n8n_workflow_execution_duration_seconds_bucket)` — inventário de buckets |
