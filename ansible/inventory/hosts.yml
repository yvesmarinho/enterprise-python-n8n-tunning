---
# ansible/inventory/hosts.yml
# Inventário Ansible — enterprise-python-n8n-tunning
# Projeto: F16 + F17 + F18 (ANA-001 / P1)
# NOTA: Credenciais e chaves SSH NÃO pertencem a este arquivo.
#       Use ansible vault ou SSH agent.

all:
  children:
    wfdb01:
      hosts:
        wfdb01.vya.digital:
          ansible_host: 86.48.31.149
          ansible_port: 5010 # SSH SPA port (fwknop)
          ansible_user: archaris

    wf001:
      hosts:
        wf001.vya.digital:
          ansible_host: 31.220.103.208
          ansible_port: 5010 # SSH SPA port (fwknop)
          ansible_user: archaris

    wfdb02:
      hosts:
        wfdb02.vya.digital:
          ansible_host: 82.197.64.145
          ansible_port: 5010 # SSH SPA port (fwknop)
          ansible_user: archaris
